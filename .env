# === QSM-008 YouTube Pattern Learning Agent ===
YOUTUBE_API_KEY=your_youtube_api_key_here
OPENAI_API_KEY=your_openai_api_key_here

# === FAISS / LangChain Memory (already active) ===
# No extra API keys needed unless using external memory storage

# === Whisper (Local Model) ===
# No API key required, but ffmpeg must be installed and available in PATH

# === Render Deployment Monitoring ===
RENDER_API_KEY=your_render_api_key_here
RENDER_SERVICE_ID=srv-xxxxxx123456 # your specific Render service ID

# === Wasabi S3 Integration for Polygon Data (active in background_worker.py) ===
WASABI_ACCESS_KEY=your_wasabi_access_key
WASABI_SECRET_KEY=your_wasabi_secret_key
WASABI_BUCKET_NAME=quanta-stock-data
WASABI_ENDPOINT=https://s3.us-east-1.wasabisys.com

# === GitHub Integration (used by github_writer.py) ===
GITHUB_TOKEN=your_github_token
GITHUB_REPO=username/repo-name
GITHUB_USERNAME=your_github_username

# === Hugging Face Token (optional for loading NLP models) ===
HUGGINGFACE_API_TOKEN=your_huggingface_token_if_needed

# === Redis (Used by MeshScheduler) ===
# If using Upstash or other cloud Redis, include it
# Example:
# REDIS_URL=rediss://default:xxxxx@hostname:6379

